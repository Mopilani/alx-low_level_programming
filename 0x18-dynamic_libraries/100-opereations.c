#include <stdio.h>
/**
 * add - function that performs addition
 * @a: numb1
 * @b: numb2
 * Return: the opration's result
 */
int add(int a, int b)
{
	return (a + b);
}
/**
 * sub - function that performs substraction
 * @a: numb1
 * @b: numb2
 * Return: the opration's result
 */
int sub(int a, int b)
{
	return (a - b);
}
/**
 * mul - function that performs multiplication
 * @a: numb1
 * @b: numb2
 * Return: the opration's result
 */
int mul(int a, int b)
{
	return (a * b);
}
/**
 * div - function that performs divsision
 * @a: numb1
 * @b: numb2
 * Return: the opration's result
 */
int div(int a, int b)
{
	return (a / b);
}
/**
 * mod - function that performs modulo
 * @a: numb1
 * @b: numb2
 * Return: the opration's result
 */
int mod(int a, int b)
{
	return (a % b);
}
