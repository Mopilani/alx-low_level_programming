#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - Entry point.
 * @s: integer pointer.
 * Description: function that returns the length of a string.
 * Return: integer.
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{}
		return (i);
}
